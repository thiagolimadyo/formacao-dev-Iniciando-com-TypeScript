interface Usuario {
  id: number;
  nome: string;
  email: string;
}

const usuarioLogado: Usuario = {
  id: 10,
  nome: "Xuxa da Suilva",
  email: "x.silva@microsoft.com",
};

console.log(usuarioLogado);
